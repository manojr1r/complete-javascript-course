//type conversion
console.log(String(23), Number.parseInt("text"));

//type coercion
console.log("I am " + 23 + " years old"); //the number 23 is converted to string and then concatenated with the other strings.
console.log("23" - "10" - 3); //the strings are converted to numbers and then the subtraction is performed.
